name: Deploy
on:
  workflow_dispatch:
  push:
    branches:
      - main
jobs:
  build:
    name: Run API
    runs-on: ubuntu-latest
    steps:
      - name: Build Cucumber Framework
        run: |
          echo "Build Cucumber Framework"
          sleep 11s
